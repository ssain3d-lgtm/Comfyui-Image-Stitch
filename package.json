{
  "name": "comfyui-image-stitch-tests",
  "version": "1.2.0",
  "private": true,
  "description": "Frontend tests for the Multi Stitch Images ComfyUI node. ComfyUI itself never reads this file. The version tracks pyproject.toml so a release cannot half-happen; tests/test_packaging.py checks it.",
  "scripts": {
    "test": "node --test \"tests/web/*.test.mjs\"",
    "test:logic": "node --test tests/web/logic.test.mjs",
    "test:browser": "node --test tests/web/browser.test.mjs",
    "test:coverage": "node --test --experimental-test-coverage \"tests/web/*.test.mjs\"",
    "lint": "eslint \"web/**/*.js\" \"tests/web/**/*.mjs\" \"tests/web/stubs/*.js\""
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.10.0",
    "globals": "17.12.0",
    "playwright": "1.63.0"
  }
}
