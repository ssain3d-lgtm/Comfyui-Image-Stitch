{
  "name": "comfyui-image-stitch-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend tests for the Multi Stitch Images ComfyUI node. ComfyUI itself never reads this file.",
  "scripts": {
    "test": "node --test \"tests/web/*.test.mjs\"",
    "test:logic": "node --test tests/web/logic.test.mjs",
    "test:browser": "node --test tests/web/browser.test.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
